# Variables — Claude Provider Adapter

No environment variables, credentials, configuration files, API keys, or secrets are added. Claude authentication remains entirely inside the user's existing visible browser session.
