# Permissions — Queue Motion Polish

No permission or authorization behavior changes. The extension retains its existing `activeTab`-only MV3 surface. Deletion still requires the existing explicit review confirmation before the queue starts.
