# Tasks — Gemini Provider Adapter

- [x] Add unit tests for Gemini `/app/<id>` discovery, visible delete guards, and provider routing.
- [x] Implement `GeminiAdapter` and `createProviderAdapter()` with a shared list/delete contract.
- [x] Register Gemini in the manifest, popup host check, and package validation without changing UI.
- [x] Run automated validation and update maps with manual Gemini verification pending.
- [x] Retry Gemini action-menu discovery across hover/sidebar redraws and make failure text readable without changing normal queue layout.
- [ ] Reload the extension and manually validate a disposable Gemini conversation.
