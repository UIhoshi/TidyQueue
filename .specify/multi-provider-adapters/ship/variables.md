# Variables — Multi-Provider Adapters

No environment variables, secrets, API keys, provider credentials, or configuration values are added. The adapters operate only against the signed-in tab that the user visibly opens.