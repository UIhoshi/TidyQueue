{
  "name": "quickdel",
  "version": "0.1.0",
  "private": true,
  "description": "A local-only Chrome extension for reviewed ChatGPT conversation deletion.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "package:check": "node scripts/package-check.js"
  }
}